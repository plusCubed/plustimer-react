{
  "private": true,
  "name": "plustimer-pwa",
  "version": "0.0.0",
  "scripts": {
    "app:start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "app:build": "firebase use default && preact build && preact serve --server config --dest .",
    "app:serve": "yarn build && preact serve",
    "app:dev": "cross-env FIREBASE_ENV=development preact watch --port 5000",
    "app:deploy": "firebase use default && yarn app:build && firebase deploy --only hosting",
    "functions:build": "firebase use default && babel functions/*.es6 --out-dir .",
    "functions:dev": "concurrently \"babel functions/*.es6 --watch --out-dir .\" \"yarn functions:dev-serve\"",
    "functions:dev-serve": "firebase use staging && cross-env FIREBASE_ENV=development firebase serve --only functions --port 5001",
    "functions:deploy": "firebase use default && yarn functions:build && firebase deploy --only functions",
    "all:build": "firebase use default && yarn app:build && yarn functions:build",
    "all:dev": "concurrently \"yarn app:dev\" \"yarn functions:dev\"",
    "all:deploy": "firebase use default && yarn all:build && firebase deploy",
    "lint": "eslint src",
    "precommit": "lint-staged"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not ie<=11"
  ],
  "eslintConfig": {
    "extends": [
      "react-app",
      "prettier"
    ],
    "rules": {
      "no-unused-vars": "warn",
      "react/sort-comp": "off",
      "lines-around-comment": "off",
      "linebreak-style": "off",
      "object-shorthand": "off",
      "no-use-before-define": "off",
      "no-undef": "warn",
      "prefer-destructuring": "warn"
    }
  },
  "lint-staged": {
    "{src,functions}/**/*.{js,json,css,md,es6}": [
      "prettier --write",
      "git add"
    ]
  },
  "eslintIgnore": [
    "build/*"
  ],
  "prettier": {
    "single-quote": true
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^8.2.1",
    "babel-plugin-async-to-promises": "^1.0.5",
    "babel-plugin-transform-regenerator": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "concurrently": "^3.5.1",
    "copy-webpack-plugin": "^4.4.1",
    "cross-env": "^5.1.3",
    "eslint": "^4.18.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-config-react-app": "^2.1.0",
    "eslint-plugin-flowtype": "^2.45.0",
    "eslint-plugin-import": "^2.8.0",
    "eslint-plugin-jsx-a11y": "^6.0.3",
    "eslint-plugin-react": "^7.6.1",
    "fast-async": "^6.3.0",
    "flow-bin": "^0.66.0",
    "husky": "^0.14.3",
    "if-env": "^1.0.4",
    "lint-staged": "^6.1.1",
    "preact-cli": "^2.1.3",
    "preact-cli-plugin-fast-async": "^1.0.1",
    "preact-cli-plugin-flow": "^0.2.0",
    "preact-cli-sw-precache": "^1.0.3",
    "prettier": "^1.10.2",
    "rimraf": "^2.6.2",
    "webpack-bundle-analyzer": "^2.10.0",
    "worker-loader": "^1.1.0"
  },
  "dependencies": {
    "asyncro": "^3.0.0",
    "date-fns": "^1.29.0",
    "firebase": "^4.10.0",
    "mitt": "^1.1.3",
    "preact": "^8.2.7",
    "preact-compat": "^3.18.0",
    "preact-material-components": "^1.3.5",
    "preact-portal": "^1.1.2",
    "preact-router": "^2.6.0",
    "preact-slots": "^1.0.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-media": "^1.8.0",
    "unistore": "^3.0.4"
  }
}
